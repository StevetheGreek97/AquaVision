PyQt6==6.9.0
QtAwesome==1.4.0
pyqtgraph==0.13.7
numpy>=1.26,<2  # sam3 requires numpy<2
opencv-python==4.11.0.86
pandas==2.2.3
Pillow==11.2.1
psutil==7.0.0
PyYAML==6.0.2
shapely==2.1.0
ultralytics==8.3.167
# SAM2 — installed separately in Dockerfile from GitHub
# SAM3 — installed separately (git+https://github.com/facebookresearch/sam3.git + einops + pycocotools);
#        checkpoint sam2_configs/sam3.pt is gated on https://huggingface.co/facebook/sam3
# DEXTR  — installed separately in Dockerfile from vendor/
